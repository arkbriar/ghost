[project]
name = "time-pressure-agent-loop"
version = "0.1.0"
description = "Continuous timer-driven agent loop using OpenAI Responses API."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.60.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
